#include <stdio.h>

int main() {
    int x = 30;  
    int y = 40;  
    int result;

    result = (x - y) * (x - y);

    printf(" x = %d and y = %d result is: %d\n", x, y, result);

    return 0;
}
