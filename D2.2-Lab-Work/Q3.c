#include <stdio.h>

int main() {
    int h = 4; 
    int b = 6;   
    int area;

    area = (b * h) / 2;

    printf("Area of the triangle with base  %i  height %i is: %i\n", b, h, area);

    return 0;
}
